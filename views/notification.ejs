<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contractor profile</title>
    <link rel="stylesheet" href="/stylesheets/notification.css">
</head>

<body>
    <section class="header">
        <div class="navbar">
            <div class="image"><img src="/images/contractor.jpg" alt=""></div>
            <div class="navbartext">
                <a href="/contractorprofile">Home🏠</a>
                <a href="/createworker">Add Workers👷‍♂️</a>
                <a href="/viewworker">View Workers👀</a>
                <a href="/allocateworker">Allocate Workers📋</a>
                <a class="active" href="/notification">Notifications🔔</a>
                <a href="/login">Logout🚪</a>
            </div>
        </div>
    </section>
    <div class="main">
        <h1>MESSAGES</h1>
        <% if(messageresults && messageresults.length>0 ){%>
            <ol>
                <% messageresults.forEach(message=> { %>
                    <li> message from:-<%= message.company_username %> <br>
                            message:-<%= message.message%> <br>
                                domain:-<%= message.worker_field %> <br>
                                    requirement:-<%= message.numberofworkers %>workers <br>
                                        required for:-<%= message.noofdays%> days <br>
                                            message id:-<%=message._id%> <br>
                                                <% if (message.message_status==="pending" ){ %>
                                                    <a class="reply" href="/replymessage">reply message</a>
                                                    <% } %>
                    </li>
                    <% }); %>
            </ol>
            <br>

            <% } else{%>
                <p>no messages</p>
                <%}%>
    </div>
</body>

</html>